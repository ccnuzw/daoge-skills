# 工程与本地环境

<!-- 填写说明：写“新人 30 分钟能跑起来”所需的最少信息；不要写产品规则，不要记录真实密钥。 -->

## 1. 当前目录

```text
<仓库根>/
  <前端或客户端目录>/
  <后端或服务目录>/
  {{文档目录}}/          # 文档中心
  scripts/              # 开发与文档检查脚本
```

## 2. 技术栈

| 层 | 技术 | 版本 |
| --- | --- | --- |
| 后端 | | |
| 前端 | | |
| 数据库 | | |
| 缓存/队列 | | |

## 3. 默认端口

| 服务 | 端口 | 说明 |
| --- | --- | --- |

## 4. 环境变量

| 变量 | 必填 | 默认 | 说明 |
| --- | --- | --- | --- |
| `DATABASE_URL` | 是 | — | <连接串格式> |
| <VAR> | | | |

从示例文件创建本地配置；示例值必须可公开，真实密钥不得提交。

## 5. 本地启动

```sh
<安装依赖命令>
<初始化数据库/环境变量命令>
<启动开发环境命令>
```

## 6. 构建与静态检查

| 命令 | 作用 |
| --- | --- |
| `<测试命令>` | 运行测试 |
| `<类型检查命令>` | 类型/静态检查 |
| `node scripts/check-docs.mjs` | 文档结构与链接检查 |

## 7. 数据库与迁移

- migration 目录：`<路径>`；新增迁移的命名与执行方式：<...>。
- 本地重建数据库的方式：<...>。

## 8. Request ID 与日志

- 所有入口生成或透传 request_id；日志中可用于关联一次请求。
- 日志级别与位置：<...>；敏感字段禁止输出。

## 9. 新功能落点

| 类型 | 落点 |
| --- | --- |
| 接口 | <目录> |
| 领域逻辑 | <目录> |
| 前端页面 | <目录> |
| 测试 | <目录> |

## 10. 常见问题

- <问题>：<表现> → <解决方式>
