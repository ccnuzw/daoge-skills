# 第 1 批施工单 · 纯前端批（画布 + 检查器 + 交互 + 文案）

> 依据：方案文档 `studio-creator-centric-refactor-plan-zh.md`（下称「方案」）+ 实施规格书 `daoge-pic-upgrade-spec-zh.md`（下称「规格书」）
> **本批性质：纯前端。不碰后端、不碰 schema、不碰鉴权表。** 这是本批最大的价值——风险最低、见效最快、可以随时停。

---

## 进度快照（**每次做完一组就在这里更新一行；换会话时先读这一节**）

| 日期 | 进度 | 下一步 |
|---|---|---|
| 2026-09-17 | **已开工**：A1 开工前的守卫核查完成（读了 `workbench-route.test.js` 10 个测试），**发现方案 7.7.2 的收益被高估** → A1 降级为小改（见第 8 节日志第 1 条，待确认改方案） | 确认后实施 A1 小改，再进 A2 |
| 2026-09-17 | **A1 完成**（小改版）：新增派生函数 `contextLevelOf`、注释澄清 `roundId` / `assetScope` 的角色、新增一致性守卫测试；方案 7.7.2 已按纪律回改 | 进 **A2**（视图注册表：先写守卫桩，见 0.3-②） |
| 2026-09-17 | **A2 完成**：新增「视图 → 宿主」单一来源（`VIEW_HOSTS` / `PRIMARY_VIEWS` / `viewsHostedBy`）+ 守卫锁一致性（3 条）；未改导航组件 | 进 **A3**（共享翻译表：先写桩，见 0.3-①） |

> **交接约定**：换会话后，新会话只需读 **本节 + 第 8 节实施日志** + 项目记忆 `<workspace>/.workbuddy/memory/YYYY-MM-DD.md`，
> 即可无损接续。**没写进这三处的东西等于没发生。**

---

## 0. 前置

**本批是否触发规格书改动：否**（但**细化**了规格书 5.4 的一条归属，见下）

**细化说明（必须记录）**：规格书 5.4 把「枚举→人话翻译表 + 搜索 label 修正」整体归了一批。
核查后拆分：**前端侧**（翻译表 + 前端不再直接渲染 `result.purpose` 英文）留在一批；
**后端侧**（`queries.ts` 拼 label 时不再直接拼英文 `round.purpose`）**移到二批**，
因为那要动 `src/vnext/domain/queries.ts`，与本批「纯前端」的边界冲突。**规格书 5.4 的文字不做修改**（它描述的是完整工作），
本施工单记录这个拆分的理由——**这属于"细化"，不是"违背"**。

### 0.1 本批会破坏的守卫（先写桩，再动代码）

| 守卫 | 为什么会红 | 处理 |
|---|---|---|
| `terminology-guard.test.js` | 「轮次→批次」进术语单后，扫描会命中现有文案 | **顺序做了防控制**：先把文案改完（D 组之后），**最后一步**才加词 |
| `workbench-route.test.js` | **A1 已降级为小改**（见第 8 节日志）：只加派生函数、**不动**守卫逻辑 → **预期本测试不需要改**；若它变红，说明改过头了，退回 |
| `phase4-navigation-registry.test.js` | 视图注册表（A2） | 扩展而非放宽：新增「宿主视图 / 是否一级入口 / 所需上下文」断言 |
| `source-text-guard.test.js` | 前端源码文本级断言 | 逐条核对，属于「文本变了所以断言跟着变」，**不许删断言** |

### 0.2 本批会触及、需逐条复核的守卫（规格书 3.2 的「其余按批核对」）

- **画布 5 个**：`lineage-accessible-model` / `lineage-data-loader` / `lineage-minimap` / `lineage-viewport-model` / `round-lineage`
- **挑图 3 个**：`selection-model` / `workbench-bulk-ui` / `project-selection-api`
- **搜索 1 个**：`phase4-search`
- **路由 1 个**：`phase4-route-refresh`

### 0.3 新增守卫桩（本批必须新建，先写成 fail）

1. **翻译表是唯一来源**——断言 `main.jsx` 与 `studio-search.jsx` 都从**同一个共享模块**取 purpose 的人话 label，
   且两处都不再出现硬编码的枚举→文案映射（防「选片翻译了、搜索裸奔」重演）。
2. **视图注册表完整性**——每个视图在注册表里都有「宿主视图 / 是否一级入口 / 所需上下文」三项，
   且一级入口集合与 `workbenchNavigationViews()` 一致。
3. **批次节点折叠有交互**——断言画布里存在**切换节点折叠的入口**（防「`collapsed` 字段存了却没有 UI」重演，见方案 4.3 的核查修正）。
4. **空状态三形态文案各不相同**——断言「没项目 / 搜索无果 / 筛选无果」三处用的是**三句不同的话**
   （现状是三处共用「还没有匹配项目。」，方案 4.7）。

### 0.4 环境与影响提示

- 前端改动经 `build:workbench` 产出、挂在根路径 `/assets/`，**不需要重启 daemon** → 改动**立即**在你的日用界面上可见。
- **本批期间你的日常界面会持续变化**——这是预期的（本批就是在改界面）。若要一个安静的环境，先按规格书第 5 节起独立 workspace + 端口。
- **不动数据**：本批只改前端代码与文案，**不写库、不迁数据**，旧数据原样可用。

---

## 1. 目标（可验收的一句话）

**画布从「349 个方块的谱系图」变成「按批次折叠、只铺该铺的、引用线自动画出来的主界面」，
并且挑图、检查、确认、发起都能在画布上完成——全程不离开画布。**

---

## 2. 任务分解

### A 组 · 基础设施（先做，避免后面大面积返工）

| # | 任务 | 涉及文件 | 依据 |
|---|---|---|---|
| **A1** | **路由小改**（幅度已按施工核查下调，见第 8 节日志）：① 加**显式派生函数** `contextLevelOf(route)`；② 消除 `roundId` ≡ `compareRoundIds[0]` 冗余；③ 注释澄清 `assetScope` = 筛选范围。**不动**降级守卫与 `normalizeRoute` 的判定逻辑 | `web/src/workbench-route.mjs`（必要时才碰调用方） | 方案 7.7.2（已修正） |
| **A2** | **视图注册表**：把 `WORKBENCH_VIEWS`、`main.jsx` 的 `viewRenderers`、`ACTIVE_VIEWS`（高亮归属）三到四处收成**一处声明** | `web/src/workbench-route.mjs`、`workbench-navigation.jsx`、`main.jsx` | 方案 7.8.2 |
| **A3** | **枚举→人话共享翻译表**（前端侧）：新建共享模块（如 `purpose-labels.mjs`），`main.jsx` 的 `ROUND_PURPOSE_OPTIONS` 与 `studio-search.jsx` 共用；搜索结果不再直接渲染 `result.purpose` 英文 | 新建 `web/src/purpose-labels.mjs`、`main.jsx`、`studio-search.jsx` | 方案 7.9.1（后端侧移二批） |
| **A4** | **「轮次→批次」改词**（本批最大的一块文案工作，约 18 文件 113 处） | 全部前端 | 方案 7.9.2 |

### B 组 · 画布核心（本批最大的一块）

| # | 任务 | 要点 | 依据 |
|---|---|---|---|
| **B1** | **折叠到批次级** | 建「节点折叠交互」（`collapsed` 字段已在、随布局持久化，**缺的是切换 UI**）；默认一批次 = 一节点（图 + 「共 N 张」），双击展开 | 方案 4.3 第一刀 |
| **B2** | **去冗余** | 只铺 task / round / asset；`run_item`、`plan` 不上画布（数据保留，检查器里可见） | 方案 4.3 第二刀 |
| **B3** | **自动分组与连线** | 按轮次自动成组写 `canvas_groups`；按 `parent_round_id` 自动画引用线写 `canvas_links`（经既有 `POST /api/projects/:id/canvas-layout`，**不新增端点**） | 方案 4.3 第三刀 |
| **B4** | **增量插入布局** | 新节点插进已有布局、**不重排已有**；「自动整理」作为**用户主动触发**的动作 | 方案 4.3 / 7.3 |

### C 组 · 交互细节

| # | 任务 | 要点 | 依据 |
|---|---|---|---|
| **C1** | **检查器** | 属性 / 过程资产（计划·提示词·历史，可复制导出）/ **确认入口**（待确认时）/ **编辑计划入口**（机制已对，缺界面；改后必须提示「计划已更新，请重新确认」）/ 窄窗口退化抽屉 | 方案 4.5、8.9#20 |
| **C2** | **菜单重设** | 工具栏管「怎么看」、右键管「对它做什么」、菜单**随对象状态变**（候选/已选定/已交付三套）；选中时浮现同内容浮条 | 方案 4.4 |
| **C3** | **挑图体验** | 快捷键（`←→` / `空格` / `X` / `Enter` / `Esc`）、**3–4 张对比**、**缩放上限提到 ≥4×**（现为 0.75–2×）**+ 全屏**、挑完就地「按这几张再来」 | 方案 4.8（含 2026-09-17 核查修正）、8.9#19 |
| **C4** | **出图占位符与动画** | 按计划的 `item_count` 先立占位格、每出一张填一张；批次节点上就地「暂停 / 取消」 | 方案 4.9 |
| **C5** | **「出完了叫我」**（2026-09-17 认可并入） | 离开画布后出图完成的感知：**标题栏未读数**（不依赖权限，主方案）+ **浏览器通知**（需授权；未授权则静默降级为仅标题栏）；出完后未读清零的时机 = 用户回到画布 | 方案 9.5 |

### D 组 · 状态与文案（**A4 的加词放最后**）

| # | 任务 | 要点 | 依据 |
|---|---|---|---|
| **D1** | **首屏三形态** | 没项目 / 有项目没选 / 选了，三套不同表现；**修空状态**（「还没有匹配项目。」是搜索无果的说法，不能给全新用户看） | 方案 4.7 |
| **D2** | **失败文案** | 四类状态四种说法（`failed` / `blocked` / `cancelled` / `partial`）、分清「我的问题 / 系统的问题」、**融进批次状态不弹窗** | 方案 4.10 |
| **D3** | **质量指标迁检查器** | 从 `project-overview` 面板搬到检查器 / 列表视图（**能力不删，只换位置**） | 方案 7.10.2 |
| **D4** | **最后一步：把「轮次」加进术语单** | `terminology.mjs` + `docs/daoge_pic_terminology_zh.md` **必须同时改**（守卫要求两者一一对应）；加完守卫开始锁住它 | 方案 7.9.2 |

### 收尾

类型检查（`tsconfig.web.json` 第 1 档清零）→ `npm run lint` 全量 0 error → 双端 typecheck → 全套回归 → `cmp` 线上产物与磁盘。

---

## 3. 守卫变更（汇总）

- **改**：`terminology-guard`（D4）、`workbench-route.test`（A1）、`phase4-navigation-registry`（A2，扩展）、`source-text-guard`（全批）
- **复核**：`lineage-*`（5）、`selection-model` / `workbench-bulk-ui` / `project-selection-api`（C3）、`phase4-search`（A3）、`phase4-route-refresh`（A1）
- **新增 4 个桩**（见 0.3）

---

## 4. 迁移或切换步骤

**无。** 本批不动 schema、不动数据、不需要迁移或切换。

---

## 5. 验收清单

- [ ] 画布上单批次 = 一个节点，标着「第 N 批 · 完成 · X 张」；**双击能展开看到图**，再双击收起
- [ ] 展开的批次里**只有图**，没有「运行项 / 计划」节点
- [ ] 批次之间的**引用线自动出现**（有上游关系的地方）
- [ ] 新增一批后，**已有节点的位置没有挪动**（增量插入生效）；点「整理」才重排
- [ ] 选中一个批次 → **检查器出现**：属性、计划、待确认时的确认按钮、编辑计划入口
- [ ] 右键菜单**随对象状态变**（候选 / 已选定 / 已交付 三套菜单内容不同）
- [ ] 挑图：`→` 切下一张、`空格` = 就它了、`X` = 不行、`Enter` 放大；**3–4 张能并排比**；**能放大到 4× 以上**
- [ ] 出图时**占位格先立起来**，出一张填一张
- [ ] 全新用户首屏是「新建第一个项目」，**不是**「还没有匹配项目。」
- [ ] 批次失败时显示「完成 · X 张没成」+「重试这 X 张」，**没有弹窗**
- [ ] 质量指标在检查器 / 列表里能找到（不在 `project-overview` 独占）
- [ ] **切去别的标签页 / 别的应用，出图完成后标题栏出现未读提示**；已授权通知的会收到系统通知（未授权不报错、不反复索要）
- [ ] 全局搜索里**不再出现英文枚举**（如 `refinement`）
- [ ] 界面上不再出现「轮次」字样（该说「批次」的地方全部改到）
- [ ] `tsconfig.web.json` 第 1 档仍为 0 错；`lint` 全量 0 error；全套回归绿

---

## 6. 端到端检查

**本批不是收尾批**，不跑规格书第 8 节的完整 DoD。但**第 1、4、5 条要顺带过一遍**
（首屏三形态 / 占位符与暂停取消 / 挑图快捷键与对比）——它们恰好是本批交付的。

---

## 7. 开放项销账（方案第 8、9 节中与本批相关的）

| 项 | 本批如何覆盖 |
|---|---|
| **#17 空批次显示** | D2 覆盖：「0 张 · N 张没成」+ 重试按钮 |
| **#18 43 轮次超屏** | B1 覆盖：横向滚动 + 默认收起最老批次 + 按时间筛选 |
| **#19 快捷键与输入框焦点冲突** | C3 覆盖：**焦点在输入框时全局快捷键全部失效**，画布快捷键只在画布聚焦时生效 |
| **#20 窄窗口检查器** | C1 覆盖：退化为右滑抽屉，选中时自动弹出 |
| **#8 双标签页** | **本批不动**——按 8.9 的结论记录为已知限制（`expectedVersion` 兜底，不坏数据） |
| **9.5 出完了叫我**（增量建议） | C5 覆盖：标题栏未读 + 可选浏览器通知 |
| **9.7 失败原因→建议映射 / 9.8 理解说明字段** | **本批不含**（9.7 属 skill 侧、9.8 动计划契约 → 二 / 三批） |

**⚠️ 说明**：方案第 9 节 10 条增量建议中，**9.5「出完了叫我」已于 2026-09-17 获认可、并入本批 C5**；
**其余 9 条仍为「建议」**，未并入任何批次（需要时逐条认可后再入批）。

---

## 8. 实施日志

### 2026-09-17 · A1 开工前的核查：**方案 7.7.2 的收益被高估**（需回头改方案）

读 `tests/vnext/workbench-route.test.js`（134 行 / 10 个测试）后，发现方案 7.7.2 的判断不成立。

**方案原文**：`normalizeRoute` 那 41 行里「一半在**防御一个本不该存在的歧义**」，且「拆成两个字段后，**那一半守卫直接消失**」。

**实测**：那些守卫**不是在防御字段歧义**，而是在**锁住修过的真实 bug**——测试注释写得很明白：

| 守卫 | 注释里的原因（原文） |
|---|---|
| `route never keeps a context level that has nothing behind it` | 孤儿 round 曾让 context loader 报「请先选择一个任务，再继续查看轮次或运行。」 |
| scope 沿层级降级 | 「a round-scoped asset request with no round made `assetRefreshPath` return null, **so the list silently kept its old contents**」——**静默保留旧内容**，比报错更糟 |
| `assets/lineage` 深链永不保留 studio 作用域 | 跨层级混看没有意义 |
| `runId` 只在渲染 run 的视图存活 | 切 tab 带过去的 `run` 曾让页面答「请先打开生成运行视图」 |

**结论**：
1. 这些不变量**拆字段不会让它们消失**——scope 必须有支撑（scope=round 而没 roundId 就是不自洽），降级逻辑必须保留；
2. **唯一真实的收益**是 `roundId` ≡ `compareRoundIds[0]` 的冗余可以消除；
3. 所以 **A1 降级为小改**：
   - **做**：把「上下文层级」提取为**显式派生函数** `contextLevelOf(route)`（为 B 组铺路）+ 在注释里澄清 `assetScope` 本就是「筛选范围」；
   - **不做**：删降级守卫、重写 `normalizeRoute` 的 41 行——那会**破坏修过 bug 的防线**。

**按规格书从属纪律**：此发现需**回头修正方案 7.7.2**（把「一半守卫消失」改成准确描述）。**待刀哥确认后修改**。

**✅ 已回改（2026-09-17，刀哥确认）**：方案 7.7.2 已改写（标题改为「有一个是冗余的」+ 新增「原版本的错误论断」小节 + 修正后的设计）；
**连带同步 4 处**：第 1 节引用、5.4 分期表、7.7.4 一览表、10.2 同步改清单；**规格书 3.2 表**同步。
**A1 按修正后的「小改」实施。**

---

### 2026-09-17 · **A1 完成**（小改版）

**实际改了四处**（`web/src/workbench-route.mjs` + 一个测试文件）：

1. **新增导出 `contextLevelOf(route)`**——由三个 id 的存在性推导层级（`round` / `task` / `project` / `studio`），
   并在 JSDoc 里写明「它**不是** `assetScope`」；
2. **用它替换 `normalizeRoute` 里原来的三元表达式**（`const contextScope = roundId ? ...`）——
   **等价替换**，把「层级」的知识集中到一处；
3. **注释澄清两处语义**：`roundId` 是 `compareRoundIds[0]` 的派生快捷方式；`assetScope` 是「筛选范围」；
4. **新增一致性守卫测试**（追加，不动原有 9 条断言）。

**关于「消除 roundId 冗余」的实测判断（与原计划不同）**：
查调用方发现——**只有 2 处**直接用 `route.roundId`（`asset-refresh-plan.mjs:15`、`use-route-refresh.mjs:5`），
但**有 10 个测试断言**它的存在。删字段的收益（少一个字段）远小于成本（改 10 个断言 + 2 处调用 + 回归风险）。
→ **改成「让它不可能不一致」**：守卫断言 `roundId === compareRoundIds[0] ?? null`。
**这才是「消除冗余」的可执行形式**——不删字段，而是**用守卫锁住一致性**，
正是项目「单一来源 + 守卫」方法论在路由上的直接应用。

**验证**：
- `node --test tests/vnext/workbench-route.test.js` → **10/10 通过**（原 9 条不变量全保持 + 新增 1 条）
- 相关回归 `source-text-guard` / `phase4-navigation-registry` / `phase4-route-refresh` / `workbench-performance` → **17/17 通过**
- `npm run typecheck:web` 通过；`npm run lint` **0 error**（58 warnings 为既有）

**结论**：A1 的**实测收益小于方案原描述**（原说「一半守卫消失」，实际一条都没动）——
但这正是修正后该有的样子：**保住防线，只做能证明等价的重构**。

---

### 2026-09-17 · **A2 完成**（保守版：新增事实来源 + 守卫锁一致性）

**先写守卫桩、确认它红**（`tests/vnext/view-registry.test.js`，3 条）——红的原因是模块里还没有 `VIEW_HOSTS`，
证明桩有效、不是假绿；然后建注册表让它变绿。

**实测缺口比原描述更精确**：方案 7.8.2 说「新加视图要手改三到四处」，实测发现——
**一级入口已有单一来源**（`workbench-navigation-model.mjs` 原有的 7 行：`STUDIO_NAVIGATION_VIEWS` + `PROJECT_NAVIGATION_VIEWS`），
真正缺的是**「视图 → 宿主」这一维**（此前由导航组件里三个手写的 `*_ACTIVE_VIEWS` Set 隐式承担）。

**做法（保守版）**：
- **新增** `VIEW_HOSTS`（14 个视图 → 宿主：4 个一级入口 + `system`）、`PRIMARY_VIEWS`（**从已有两个数组派生**，不重复定义）、`viewsHostedBy(host)`；
- **不改**导航组件——它的 `NAVIGATION_ITEMS` / `AUX_ITEMS` 携带路由细节（changes 对象），改它风险高、收益只是「少一处手改」；
- 用**守卫锁一致性**（与 A1 同一套路）：注册表键集合 ≡ `WORKBENCH_VIEWS`、`PRIMARY_VIEWS` ≡ `workbenchNavigationViews(true)`、
  宿主必须是真实一级入口或 `system`、反向守卫防孤儿登记、高亮分组与注册表一致。

**顺带发现一处现象（未修，留给后续）**：**`project-overview` 此前没有任何高亮归属**——
进入项目总览视图时左侧没有任何入口点亮（`WORKBENCH_ACTIVE_VIEWS` 只有 `projects`）。
注册表已把它归到「项目管理」，**是否要给它补高亮，是 B/C 组的交互细节，不在本项**。

**验证**：新守卫 **3/3** ✓；既有守卫（`phase4-navigation-registry` + `workbench-route`）**14/14** ✓ 未被波及；
`typecheck:web` 通过；`lint` **0 error**。

**结论**：与 A1 同一个模式——**先把语义固化成单一来源，再用守卫锁住它与既有实现的一致性**，
而不是立刻替换实现（替换的收益只是"少改一处"，风险却是动到被守卫锁定的组件）。

### 待续
