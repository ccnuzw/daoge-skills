openapi: 3.1.0
info:
  title: "{{PROJECT_NAME}} API"
  version: "{{VERSION}}"
  description: "字段级机器契约。业务语义、权限、幂等和失败边界见当前版本接口契约。"
servers: []
paths: {}
components:
  schemas:
    ErrorResponse:
      type: object
      additionalProperties: false
      required: [code, message, request_id]
      properties:
        code:
          type: string
        message:
          type: string
        request_id:
          type: string
