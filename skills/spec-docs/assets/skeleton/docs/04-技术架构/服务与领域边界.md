# {{项目名}} 服务与领域边界

<!-- 填写说明：单体项目可只保留“逻辑领域映射”与“数据访问规则”；多服务项目补全其余小节。 -->

## 1. 物理进程

| 进程 | 职责 | 生命周期 | 扩展方式 |
| --- | --- | --- | --- |

## 2. 逻辑领域映射

| 领域 | 进程 | 聚合/实体 | 对外契约 |
| --- | --- | --- | --- |

## 3. 数据访问规则

- 每张表只有一个所有者；跨领域写入必须通过显式接口或事务编排。
- 禁止直接写他人表；需要只读副本时说明同步方式与延迟容忍。
- 共享状态必须位于共享存储，水平扩展不得依赖单进程内存。

## 4. 内部接口

| 接口 | 调用方 | 鉴权 | 用途 |
| --- | --- | --- | --- |

<无内部接口时写“不适用 + 理由”。>

## 5. 一致性策略

| 场景 | 策略 | 失败处理 |
| --- | --- | --- |
| 同库事务 | <边界> | <回滚> |
| 跨域最终一致 | <异步/补偿> | <重试与对账> |

## 6. 可观测性边界

- 健康检查与就绪检查语义分离；就绪检查聚合关键依赖。
- 指标入口受部署层保护；日志包含 request_id 且不含秘密。

## 7. 本地与生产拓扑

| 环境 | 拓扑 | 与生产差异 | 可证明范围 |
| --- | --- | --- | --- |

## 8. 未来拆分触发条件

<出现什么信号才拆分；不满足时保持当前边界>

## 9. 验收口径

<边界相关的验收要求：例如跨域写入审计、依赖故障 fail-closed、所有权越权测试>