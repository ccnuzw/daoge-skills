# Host-Native 宿主接入成果归档模板

> 用途：每接完一个新宿主，就复制这份模板填一版，沉淀接入结论。  
> 目标：让后续维护者快速知道这个宿主“能不能用、怎么用、有哪些坑”。

---

## 1. 基本信息

- 宿主名称：
- 宿主类型：
  - 内置图像工具型 / 第三方工作台型 / 自建脚本批量型
- 接入日期：
- 接入人：
- 关联分支 / 会话标识：

---

## 2. 本次目标

- 这次接入要解决什么：
- 预期接到哪一层：
  - prompt 包 / 结果回填 / 全链路验证

---

## 3. 已验证能力

请按真实结果填写：

- 运行模式探测：
  - 已验证 / 未验证
- prompt 交接包：
  - 已验证 / 未验证
- `host_native_results.json` 组织：
  - 已验证 / 未验证
- schema 校验：
  - 已验证 / 未验证
- 结果回填：
  - 已验证 / 未验证
- `result_hub / review_board / completion_board`：
  - 已验证 / 未验证

---

## 4. 关键文件与字段映射

### 4.1 宿主侧输入

- 宿主实际读取哪些文件：
- 是否直接消费 `host_native_prompt_pack.json`：
- 是否只消费 `prompts.generated.json`：

### 4.2 宿主侧输出

- 结果文件路径：
- 图片输出路径规则：
- 命名规则：

### 4.3 字段映射

| DAOGE 字段 | 宿主字段/来源 | 备注 |
|-----------|---------------|------|
| `index` |  |  |
| `title` |  |  |
| `requestMode` |  |  |
| `status` |  |  |
| `output` |  |  |
| `slotId` |  |  |
| `error` |  |  |

---

## 5. 已跑命令

把真正跑过的命令贴进来：

```bash
# detect runtime

# build prompt pack

# validate host-native results

# ingest host-native results

# run smoke
```

---

## 6. 验证结果

- 关键输出文件是否生成：
- smoke 是否通过：
- 是否存在人工绕过步骤：
- 是否还有未验证路径：

---

## 7. 已知限制

请只写真实限制：

- 限制 1：
- 限制 2：
- 限制 3：

---

## 8. 后续建议

- 这个宿主适合什么场景：
- 不适合什么场景：
- 下次再接入时，优先复用哪份文件：

---

## 9. 最终结论

三选一即可：

- 可作为正式支持宿主
- 可作为实验性支持宿主
- 暂不建议接入
