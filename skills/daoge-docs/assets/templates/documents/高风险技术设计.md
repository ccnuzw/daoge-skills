---
feature_id: {{FEATURE_ID}}
status: draft
owner: unassigned
updated: {{DATE}}
authority: {{FEATURE_ID}} 的高风险实现与失败语义
---

# {{FEATURE_NUMBER}} {{FEATURE_NAME}} - 技术设计

## 1. 入口与所有权

| 项目 | 结论 | 依据/验证 |
| --- | --- | --- |
| 真实入口 | [待填写] | [待填写] |
| 调用主体与权限 | [待填写] | [待填写] |
| 唯一写入者 | [待填写] | [待填写] |
| 读取者与禁止访问 | [待填写] | [待填写] |

### 函数/用例签名

```text
function {{FEATURE_ID}}(input, actor, clock, dependencies) -> Result<Output, StableError>
前置： [待填写]
后置： [待填写]
不变量： [待填写]
副作用： [待填写]
```

## 2. 前置条件、后置条件与不变量

| ID | 类型 | 条件/不变量 | 验证位置 | 失败结果 |
| --- | --- | --- | --- | --- |
| INV-01 | 前置 | [待填写] | [待填写] | [待填写] |
| INV-02 | 后置 | [待填写] | [待填写] | [待填写] |
| INV-03 | 不变量 | [待填写] | [待填写] | [待填写] |

## 3. 状态机与稳定分支

| 分支 ID | 条件 | 决策 | 副作用 | 失败结果 |
| --- | --- | --- | --- | --- |
| B{{FEATURE_NUMBER}}-01 | 前置/权限失败 | 拒绝 | 无业务写入 | 稳定错误 |
| B{{FEATURE_NUMBER}}-02 | 重复/幂等键已存在 | 返回原结果或稳定冲突 | 不重复副作用 | [待填写] |
| B{{FEATURE_NUMBER}}-03 | 主流程守卫满足 | 执行唯一事务 | [待填写] | [待填写] |
| B{{FEATURE_NUMBER}}-04 | 依赖超时/未知结果 | 重试、补偿或保留处理中 | [待填写] | [待填写] |

分支 ID 必须稳定，日志、指标、测试、AC 和机器证据引用同一 ID；不适用分支必须写出理由和重新评估条件。

## 4. 事务、并发与幂等

| 场景 | 锁/唯一约束 | 事务边界 | 重复语义 | 失败/回滚 |
| --- | --- | --- | --- | --- |
| 正常提交 | [待填写] | [待填写] | [待填写] | [待填写] |
| 并发请求 | [待填写] | [待填写] | [待填写] | [待填写] |
| 重放事件 | [待填写] | [待填写] | [待填写] | [待填写] |

## 5. 外部调用、重试与补偿

| 调用 | 请求前状态 | 超时/重试 | 未知结果 | 补偿与告警 |
| --- | --- | --- | --- | --- |
| [待填写] | [待填写] | [待填写] | [待填写] | [待填写] |

## 6. 安全与可观测性

- [ ] 权限在业务边界重新校验，不能只依赖前端或上游网关。
- [ ] 秘密、个人数据和完整请求不会写入日志、指标标签或证据。
- [ ] 每次结果可由 request_id、actor、规则版本、分支 ID、状态前后值和结果解释。
- [ ] 失败类别稳定，重试、降级和补偿可观察但不会暴露敏感信息。

| 观测字段 | 来源 | 脱敏规则 | 用途 |
| --- | --- | --- | --- |
| [待填写] | [待填写] | [待填写] | [待填写] |

## 7. 迁移、兼容与回滚

| 变更 | 向前兼容 | 回退/恢复 | 数据保留 | 验证证据 |
| --- | --- | --- | --- | --- |
| [待填写] | [待填写] | [待填写] | [待填写] | [待填写] |

### 伪代码

```text
function execute(input, actor, clock, deps):
    REQUIRE validate_input(input)                                  // B{{FEATURE_NUMBER}}-01
    REQUIRE authorize(actor, input)                                // B{{FEATURE_NUMBER}}-01
    snapshot = read_authoritative_state(input)                     // [待填写]
    IF idempotency_hit(snapshot, input.key):
        RETURN prior_result                                        // B{{FEATURE_NUMBER}}-02
    BEGIN TRANSACTION
        guard(snapshot)                                            // B{{FEATURE_NUMBER}}-03
        apply_domain_change(input)                                 // [待填写]
        record_audit_and_observation()                             // [待填写]
    COMMIT
    IF external_side_effect_required:
        dispatch_idempotent_side_effect()                         // B{{FEATURE_NUMBER}}-04
    RETURN result
ON dependency_timeout:
    preserve_unknown_state_or_compensate()                        // B{{FEATURE_NUMBER}}-04
```

伪代码必须明确输入、规则版本、关键分支、事务边界、外部副作用、未知结果和返回错误；不得只写自然语言流程。

## 8. 分支追踪

| 分支 ID | AC | 集成/E2E | 发布门禁 |
| --- | --- | --- | --- |
| B{{FEATURE_NUMBER}}-01 | AC01 | [待填写] | [待填写] |
| B{{FEATURE_NUMBER}}-02 | AC02 | [待填写] | [待填写] |
| B{{FEATURE_NUMBER}}-03 | AC03 | [待填写] | [待填写] |
| B{{FEATURE_NUMBER}}-04 | AC04 | [待填写] | [待填写] |
