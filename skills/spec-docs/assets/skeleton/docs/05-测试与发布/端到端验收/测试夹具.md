# {{活跃版本}} E2E 测试夹具

## 1. 命名规则

所有动态值带 `run_id`：

```text
<对象>_name=<前缀>-<run_id>
idempotency_key=<case_id>-<run_id>-<sequence>
```

密码和密钥只通过环境变量或测试运行器注入，不写入报告、截图、请求快照或本文档。

## 2. 身份夹具

| 夹具 | 创建方式 | 必须状态 | 使用用例 |
| --- | --- | --- | --- |
| `admin_active` | 种子或管理入口创建 | active 管理员 | <用例> |
| `user_new` | 注册接口动态创建 | 初始不存在 | <用例> |
| `user_active` | 注册并完成必要前置 | active、依赖数据齐备 | <用例> |

不同角色必须使用不同身份，不允许复用令牌。

## 3. 业务对象夹具

| 对象 | 固定要求 |
| --- | --- |
| <对象> | <字段、状态、唯一性要求> |

## 4. 外部依赖夹具

| 依赖 | 替身方式 | 断言 |
| --- | --- | --- |
| <外部服务> | <可控 mock 的路径与行为> | <调用次数、请求外形、失败注入> |

禁止在证据中保存完整签名 URL、Authorization 或第三方密钥。

## 5. 金额与幂等夹具

| 项目 | 建议值 | 断言 |
| --- | --- | --- |
| <金额场景> | <固定值> | <余额/流水变化> |
| <幂等键> | 每批次唯一 | 重放不重复副作用 |