{
  "name": "interactive-image-batch",
  "version": "1.0.0",
  "description": "如果你是第一次使用这套 skill，先不要直接读完整 README。",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "npm run test:unit && npm run test:contracts && npm run test:integration && npm run test:artifacts && npm run test:smoke",
    "test:unit": "node --test tests/unit/*.test.js",
    "test:contracts": "node --test tests/contracts/*.test.js",
    "test:integration": "node --test tests/integration/*.test.js",
    "test:artifacts": "node --test tests/artifacts/*.test.js",
    "test:smoke": "bash scripts/run_smoke_tests.sh"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "playwright": "^1.60.0"
  }
}
