# 01 <功能名>：技术设计

<!-- 填写说明：仅高风险功能使用；只冻结关键执行逻辑，不复制 API 字段或数据模型；冲突时按权威来源与决策流程处理。主文档保留七段结构并链接本文件。 -->

来源：[01-功能主文档](01-功能主文档.md)（需求、接口、数据、验收细节以主文档为准）

## 单元契约

| 函数 | 前置条件 | 返回 | 副作用 |
| --- | --- | --- | --- |
| `<函数签名>` | <调用前必须成立> | <成功/稳定失败结果> | <事务内写库、外部调用、异步副作用；无则“无”> |

## 伪代码

```text
FUNCTION <name>(<arg>: <Type>) -> Result<<ReturnType>>:
  REQUIRE <不变量>                                     // B01-01
  BEGIN TRANSACTION
    row = DB.lock_<entity>(<key>)
    IF <重复请求>: RETURN prior_result                  // B01-02
    IF <资金/权限/状态校验失败>:
      ROLLBACK; RETURN Err(<code>, "<error_key>")       // B01-03
    DB.<write>(...)
    ASSERT <关键不变量，例如余额非负>
  COMMIT
  // ASYNC: <不阻塞主流程的副作用>
  RETURN <结果>
```

## 分支到测试追踪

| 分支 | 功能 AC | 跨功能验收 |
| --- | --- | --- |
| B01-01 | AC01 | {{活跃版本}}-E2E-01 |
| B01-02 | AC04 | 重放回归 |
| B01-03 | AC03 | <发布门禁/NFR> |

## 约束备注

- <数据库约束或锁必须是最终兜底；仅应用层先查后写不满足本设计。>
- <秘密不得出现在日志、错误、列表或后续查询中；测试必须包含秘密扫描。>
- <分支未映射 AC 时不满足 Definition of Ready。>