# {{活跃版本}} E2E 用例执行矩阵

本矩阵将[端到端验收规范]({{活跃版本}}-端到端验收规范.md)的业务用例落到执行资产。

| 用例 | 真实入口 | 必须真实的本系统依赖 | 允许的外部替身 | 最终业务断言 | 目标测试文件 | 当前证据状态 |
| --- | --- | --- | --- | --- | --- | --- |
| E2E01 <名称> | <接口/页面> | <数据库/鉴权/事务> | <替身或“无”> | <可断言结果> | `<文件路径>` | 未执行 |

状态用词：未执行 / local-mock Passed（YYYY-MM-DD）/ staging Passed（YYYY-MM-DD）/ production-gate Passed（YYYY-MM-DD）/ Pending / 待本次验证。环境与日期必须同时写，避免跨轮次误读。

## 附加安全与运维门禁

| 用例 | 覆盖内容 | 最终断言 | 当前证据状态 |
| --- | --- | --- | --- |
| SEC01 | <安全场景> | <断言> | 未执行 |
| OPS01 | <运维场景> | <断言> | 未执行 |

## 最低断言维度

每个测试文件至少覆盖：

- HTTP 状态和业务 code/error_key。
- `request_id` 存在且可用于日志关联。
- 响应不包含密码、完整 Key、上游密钥和支付密钥。
- 关键数据状态及关联 ID。
- 失败场景没有额外上游调用、扣费或重复副作用。
- 测试结束时输出用例、run_id、构建标识和报告位置。

## 缺口清单

| 用例 | 缺口 | 阻塞项 | 计划 |
| --- | --- | --- | --- |
| | | | |