# {{活跃版本}} E2E 验证证据

只追加、不覆盖；失败批次保留原记录。

```text
批次 run_id：<版本>-YYYYMMDD-HHMM-<build>
验证日期：YYYY-MM-DD
环境：local / staging / production-gate
依赖模式：<mock / real，按依赖分别标注>
代码版本：commit 或构建标识
命令/用例：<执行的规范命令>；<用例逐项：E2E01 通过；E2E02 失败：<原因>>
结果：通过 / 失败 / environment_failed
证据：<报告文件路径、日志片段位置>
遗留风险：...
```

## 记录

### YYYY-MM-DD <环境>

<按上述格式填写>

## 当前证据状态

| 用例范围 | 最新批次 | 环境 | 状态 |
| --- | --- | --- | --- |
| E2E01～ | | | |